datadog_api_key: "{{ vault_datadog_api_key }}"
redmine_db_password: "{{ vault_redmine_db_password }}"
datadog_checks:
  http_check:
  init_config:
  instances:
    - name: Example website
    - url: https://climbingchevalier.ru/
